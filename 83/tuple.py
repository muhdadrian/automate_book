# The Tuple Data Type

# tuples are different from lists. Tuples like strs, are immutable. Tuples cannot have their values modified, appended or removed.

eggs = ('hello', 42, 0.5)
eggs[1] = 99
print(eggs) # this will output error