# the split() method called on a str value and returns a list of strs.

print('My name is Simon'.split())

# by default, the str 'My name is Simon' is split wherever whitespace chars such as the space, tab, or newline chars are found.

# these whitespace chars are not included in the strs in the returned list.